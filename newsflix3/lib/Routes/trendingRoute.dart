import 'package:flutter/material.dart';

class TrendingRoute extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(
        title: Text("Trending News"),
          backgroundColor: Colors.indigo[700],
      ),
    );
  }
}